
terraform {
 required_providers {
   aws = {
     source = "hashicorp/aws"
   }
 }

 backend "s3" {
  bucket         = "dunhuang-terraform-bucket"
  key            = "dunhuang/terraform/s3/bucket05/terraform.tfstate"
 }
}

provider "aws" {
 region = "eu-central-1"
}
